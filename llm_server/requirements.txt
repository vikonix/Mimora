# LLM Server dependencies
# Install with: python -m pip install -r requirements.txt
#
# NOTE: this installs the CPU-only build of llama-cpp-python (recent versions
# have no prebuilt wheels on PyPI, so pip silently compiles without CUDA).
# For GPU (CUDA) support install the prebuilt CUDA wheel INSTEAD — see
# "llama-cpp-python with CUDA Support" in README.md:
#   python -m pip install llama-cpp-python==0.3.4 --force-reinstall --no-cache-dir --only-binary=:all: --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cu121
#   python -m pip install nvidia-cuda-runtime-cu12==12.1.105 nvidia-cublas-cu12==12.1.3.1

fastapi>=0.110.0
uvicorn>=0.29.0
llama-cpp-python>=0.2.0
